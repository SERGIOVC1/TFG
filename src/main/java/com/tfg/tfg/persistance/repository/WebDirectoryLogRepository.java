package com.tfg.tfg.persistance.repository;

import com.tfg.tfg.persistance.model.WebDirectoryLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WebDirectoryLogRepository extends JpaRepository<WebDirectoryLog, Long> {
}
