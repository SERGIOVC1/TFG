package com.tfg.tfg.persistance.repository;

import com.tfg.tfg.persistance.model.HoleheLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HoleheLogRepository extends JpaRepository<HoleheLog, Long> {
}
